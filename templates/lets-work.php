<section id="about">

    <div class="wrapper">

      <?php 
           // Custom widget Area Start
           if ( !function_exists('dynamic_sidebar') || !dynamic_sidebar('work_1') ) : ?>
          <?php endif;
          // Custom widget Area End
          ?>  

         <?php 
           // Custom widget Area Start
           if ( !function_exists('dynamic_sidebar') || !dynamic_sidebar('work_3') ) : ?>
          <?php endif;
          // Custom widget Area End
          ?>  

        

          <?php 
           // Custom widget Area Start
           if ( !function_exists('dynamic_sidebar') || !dynamic_sidebar('work_2') ) : ?>
          <?php endif;
          // Custom widget Area End
          ?>  

          <div class="bios">

            <?php 
           // Custom widget Area Start
           if ( !function_exists('dynamic_sidebar') || !dynamic_sidebar('bio_1') ) : ?>
          <?php endif;
          // Custom widget Area End
          ?>  

        <?php 
           // Custom widget Area Start
           if ( !function_exists('dynamic_sidebar') || !dynamic_sidebar('bio_2') ) : ?>
          <?php endif;
          // Custom widget Area End
          ?>  

          <?php 
           // Custom widget Area Start
           if ( !function_exists('dynamic_sidebar') || !dynamic_sidebar('bio_3') ) : ?>
          <?php endif;
          // Custom widget Area End
          ?>  

          <?php 
           // Custom widget Area Start
           if ( !function_exists('dynamic_sidebar') || !dynamic_sidebar('bio_4') ) : ?>
          <?php endif;
          // Custom widget Area End
          ?>  



          </div><!-- /.bios -->            

    </div><!-- /.wrapper -->

</section>  